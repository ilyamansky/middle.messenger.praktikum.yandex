<form id = "{{id}}">
    {{#each inputs}}
    <div>
    {{#if isLabel}}
        <label>{{label}}</label>
    {{/if}}
        <input name="{{name}}" placeholder = "{{placeholder}}" 
               type="{{type}}" value="{{value}}" class="{{class}}"
               events="{{events}}" 
        />
        </div>
    {{/each}}
</form>