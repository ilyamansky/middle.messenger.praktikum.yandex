<form>
    {{#each inputs}}
    <div>
    {{#if isLabel}}
        <label>{{label}}</label>
    {{/if}}
        <input placeholder = "{{placeholder}}" type="{{type}}" value="{{value}}" class="{{class}}" />
        </div>
    {{/each}}
</form>